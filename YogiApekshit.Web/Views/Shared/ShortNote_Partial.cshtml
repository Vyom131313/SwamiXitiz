@using Mvc.Core.ThirdParty
@using YogiApekshit.Web.Models
@using SwamiXitiz.Data.ModelsPartial
@model QA_Filter_Parameters

@{var gridId = "gridShortNotes";}
<div class="page page-dashboard">
    <section class="tile bg-greensea">
        @Html.Partial("GridToolbar", new GridToolbarModel { Title = "Write briefly five main points (in sentences).", SubTitle = "(Total Marks: 5)" })
        @(Html.ICon().Grid(gridId)
             .Url(Url.Action("GridRecords", "QA"))
            .Parameter("bookId", Model.BookId.ToString())
            .Parameter("category", Constants.Que_Categories.ShortNote.ToString())
            .Parameter("chapterNumber", Model.ChapterNumber.ToString())
            .Persistence(Persistence.Session)
            .Groupable(false)
            .Sortable(false)
            .Columns(
                new Column { Name = "Sr", Header = "Sr.", Width = 40 },
                new Column { Name = "Que", Header = "Question" },
                new Column { Name = "Ans", Header = "Answer" },
                new Column { Name = "Chapter", Header = "Chapter", Width = 70 },
                new Column { Name = "Exams", Header = "Exams", Width = 120 }
                ))
    </section>
</div>

@section scripts{
}
