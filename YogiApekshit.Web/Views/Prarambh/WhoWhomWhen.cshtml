@using Mvc.Core.ThirdParty
@using YogiApekshit.Web.Models
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Prarambh Que_1";
}

<div class="page page-dashboard">
    @Html.Partial("BreadcrumbPartial", new BreadcrumbAndTitleModel("Que1"))
    <section class="tile bg-greensea">
        @Html.Partial("GridToolbar", new GridToolbarModel { GridId = "Prarambh_Que1_Grid", Title = "State who is speaking to whom and when.", SubTitle = "(Total Marks: 9)" })
        <div ng-controller="PrarambhController">
            <span>{{QA_List_Load('WhoWhomWhen')}}</span>

            <table class="table table-condensed queAnsTable">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Statement</th>
                        <th>Who</th>
                        <th>
                            <img src="/Images/double-arrow-white.png" width="16px" />
                        </th>
                        <th>Whom</th>
                        <th>Chapter</th>
                        <th>Exams</th>
                    </tr>
                </thead>
                <tbody>
                    <tr data-ng-repeat="item in QA_List">
                        <td class="sequenceColumn">{{item.Sr}}.</td>
                        <td class="queAndansColumn">
                            <p style="line-height:1.7">
                                <b>{{item.Que}}</b> <br />
                                <i class="doublearrow-white"></i>
                                <img src="/Images/double-arrow-white.png" width="16px" />
                                <span class="whenColumn">{{item.When}}</span>
                            </p>
                        </td>
                        <td class="whoColumn">{{item.Who}}</td>
                        <td><img src="/Images/double-arrow-white.png" width="16px" /></td>
                        <td class="whomColumn">{{item.Whom}}</td>
                        <td class="chapterColumn">{{item.Chapter}}</td>
                        <td class="examColumn">{{item.Exams}}</td>
                    </tr>
                </tbody>
            </table>

            <div>
                @ViewBag.DemoID
            </div>
        </div>

    </section>
</div>

@section scripts{
}


@*
    <div class="page page-dashboard">
        @Html.Partial("BreadcrumbPartial", new BreadcrumbAndTitleModel("Que1"))

        <section class="tile bg-greensea">

            @Html.Partial("GridToolbar", new GridToolbarModel { GridId = gridId, Title = "Que1", SubTitle = "xxx" })
            <div >
                <table class="table table-condensed">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Que</th>
                            <th>Ans</th>
                        </tr>
                    </thead>
                    <tbody class='WhoIsSpeaking-result'>
                        <tr class='person' data-ng-repeat="item in QueAnsList">
                            <td>.</td>
                            <td>
                                ${{item.QUE }} <br />
                                ${{item.ANS }}
                            </td>
                            <td class="examColumn">${{item.EXAM}}</td>
                            <td></td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </section>
    </div>*@