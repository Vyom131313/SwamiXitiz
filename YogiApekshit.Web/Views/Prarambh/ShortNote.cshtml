@using Mvc.Core.ThirdParty
@using YogiApekshit.Web.Models
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Prarambh Que_4";
}

<div class="page page-dashboard">
    @Html.Partial("BreadcrumbPartial", new BreadcrumbAndTitleModel("Que4"))
    <section class="tile bg-greensea">
        @Html.Partial("GridToolbar", new GridToolbarModel { GridId = "Prarambh_Que4_Grid", Title = "Answer in short note", SubTitle = "(Total Marks: 4)" })
        <div ng-controller="PrarambhController">
            <span>{{QA_List_Load('ShortNote')}}</span>
            <table class="table table-condensed queAnsTable">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Queuestion and Answer</th>
                        <th>Chapter</th>
                        <th>Exams</th>
                    </tr>
                </thead>
                <tbody>
                    <tr data-ng-repeat="item in QA_List">
                        <td class="sequenceColumn">{{item.Sr}}.</td>
                        <td class="queAndansColumn">
                            <p style="line-height:1.7">
                                <b>{{item.Que}}</b> <br />
                                <div ng-bind-html="item.Ans | sanitize"></div>
                            </p>
                        </td>
                        <td class="chapterColumn">{{item.Chapter}}</td>
                        <td class="examColumn">{{item.Exams}}</td>
                        <td></td>
                    </tr>
                </tbody>
            </table>
        </div>
    </section>
</div>

@section scripts{
}
